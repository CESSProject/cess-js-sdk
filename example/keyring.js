const { Keyring,Converter } = require("../");
const config = require("./config");

// const accountId =
//   "brisk mimic course swallow balcony view odor agree original lion antique bridge";
// const addr = "5FFTWbBFNxggSstPbn2jfLs5KrxgxehuntFrE6JH9KpvnMjx";

// //Keyring api
// const keyring = new Keyring(config);

// // Mnemonic to PublicKey
// keyring.getPublicKeyFromMnemonic(accountId).then(console.log, console.log);

// // Wallet address to PublicKey
// keyring.getPublicKeyFromAccountId(addr).then(console.log, console.log);

// console.log(Converter.addressToEvm(addr));